Qlik Task Monitor - Prebuilt Version

How to use:

1. Edit 'config.example.yaml' and rename it to 'config.yaml'
2. Edit '.env.example' and rename it to '.env'
3. Run 'qlik_monitor.exe'

That's it! No Python installation required.
